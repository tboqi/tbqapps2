Global setup:

 Set up git
  git config --global user.name "tang boqi"
  git config --global user.email tboqi301709@gmail.com
      

Next steps:

  mkdir tbqkhn_system
  cd tbqkhn_system
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:tboqi/tbqkhn_system.git
  git push -u origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:tboqi/tbqkhn_system.git
  git push -u origin master
      

Importing a Subversion Repo?

  Check out the guide for step by step instructions.
      

When you're done:

  Continue


